# 弹幕数量控制器

- ID: `danamku_limiter`
- 作者: Heylyx841

## 功能

监听 `danmakuLoaded` 事件，在弹幕渲染前拦截处理：

- **密度限制**：按秒分桶，超出用户设定上限时自动丢弃多余弹幕（默认 5 条/秒）。
- **模糊去重**：对同秒内的弹幕先进行内容归一化去重，优先保留不重复内容；若去重后仍超出上限，则等距采样。
- 提供设置页开关与每秒上限自定义。